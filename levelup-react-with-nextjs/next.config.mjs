/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos"],
  },
};

export default nextConfig;

// import path from "path";

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   sassOptions: {
//     includePaths: [path.join(__dirname, "styles")], // 'styles' 폴더를 경로로 설정
//   },
// };

// export default nextConfig;
